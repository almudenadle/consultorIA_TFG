/**
 * Enum representing the different economic sectors a company can belong to.
 */
export enum SectorType {
    Primary = 'Primary',
    Secondary = 'Secondary',
    Tertiary = 'Tertiary',
    Quaternary = 'Quaternary',
    Other = 'Other'
}